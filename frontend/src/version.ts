// ============================================================================
// version.ts — 前端构建版本标识（第 22 轮）：
//   APP_VERSION 与 frontend/package.json 的 version 保持同步（手动维护）；
//   BUILD_TIME 为构建日期（发布时更新）。mono / night 主题均可读。
// ============================================================================

/** 版本号（与 package.json version 同步）。 */
export const APP_VERSION = '2.0.0';

/** 构建时间（发布时更新）。 */
export const BUILD_TIME = '2026-09-08';
